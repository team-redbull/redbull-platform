apiVersion: v2
name: segments-manager-mongodb
description: >-
  Platform-owned glue chart: standalone MongoDB backing segments-manager,
  deployed as its own release so it's ready before segments-manager starts.
type: application
version: 0.1.0
dependencies:
  - name: mongodb
    version: 19.1.16
    repository: oci://registry-1.docker.io/bitnamicharts
