{{- if .Values.provider.enabled }}
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: {{ .Values.provider.name }}
spec:
  package: {{ .Values.provider.package }}
  {{- if .Values.deploymentRuntimeConfig.enabled }}
  runtimeConfigRef:
    name: {{ .Values.deploymentRuntimeConfig.name }}
  {{- end }}
  {{- with .Values.provider.packagePullSecrets }}
  packagePullSecrets:
    {{- range . }}
    - name: {{ . }}
    {{- end }}
  {{- end }}
{{- end }}
