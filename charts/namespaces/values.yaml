# Namespaces this platform creates up front.
namespaces:
  - crossplane-system
  - temporal
  - segments-manager
  - bmh-system
  - server-scanner
  - redbull-workflows   # hosts the workflows + segment-connectivity releases (see helmfile)
