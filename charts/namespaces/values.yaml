# Namespaces this platform creates up front.
namespaces:
  - crossplane-system
  - temporal
  - segments-manager
  - bmh-system
  - server-scanner
  - redbull-workflows   # hosts the connectivity-workflow release (see helmfile)
